// import 'package:flutter_webrtc/flutter_webrtc.dart';
// import 'package:quagga/functions/webrtc.dart';

// Future<dynamic> handleCallerP(
//         dynamic data, RTCPeerConnection peerConnection) =>
//     handleCaller(data, peerConnection);

// void handleCalleeP(dynamic data) => handleCallee(data);

// Future<dynamic> handleSendOfferP(dynamic data, RTCPeerConnection peerConnection) =>
//     handleSendOffer(data, peerConnection);

// Future<dynamic> handleSendAnswerP(
//         dynamic data, RTCPeerConnection peerConnection) =>
//     handleSendAnswer(data, peerConnection);

// Future<dynamic> handleICEP(dynamic data, RTCPeerConnection peerConnection) =>
//     handleICE(data, peerConnection);
